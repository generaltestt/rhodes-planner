# Copy this file to .env and fill in your values.
# Locally you can also just export these before running, or pass them inline:
#   APP_PASSWORD=yourpassword GOOGLE_PLACES_API_KEY=yourkey npm start

# Password for the login screen (change this!)
APP_PASSWORD=rhodes2026!

# Google Places API (New) key. See README "Google Cloud setup" for the exact
# steps - you need a Cloud project, a card on file, and this API enabled.
GOOGLE_PLACES_API_KEY=AIzaSyBIvQZXqQMaKg5ZBd0SG8uWbEmvkwasbYw

# Optional: extra secret mixed into the auth token. Set to any random string in production.
APP_SECRET=rigaaswell!

# Optional: where the SQLite database lives. On Railway/Render set this to your
# persistent volume mount path, e.g. /data
# DATA_DIR=/data
